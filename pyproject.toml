[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "stockinsider"
version = "0.1.0"
description = "Local CLI financial analyst agent for HK + US equities — analysis only, never trades."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "typer>=0.16",
    "rich>=14",
    "pyyaml>=6",
    "jsonschema>=4",
    "openai>=1",
    "sqlite-vec>=0.1.6",
]

[project.optional-dependencies]
dev = [
    "pytest>=8",
    "pytest-cov>=6",
    "ruff>=0.12",
    "mypy>=1.16",
]

[project.scripts]
stockinsider = "stockinsider.cli.app:main"

[tool.hatch.build.targets.wheel]
packages = ["src/stockinsider"]

[tool.ruff]
line-length = 120
target-version = "py313"
src = ["src", "tools"]

[tool.ruff.lint]
select = ["E", "F", "W"]

[tool.mypy]
files = ["src"]
python_version = "3.13"
disallow_untyped_defs = true
no_implicit_optional = true
warn_redundant_casts = true
warn_unused_ignores = true

[tool.pytest.ini_options]
testpaths = ["test"]
