# Test Plan TP-007a — Agent loop core, streaming, prompts/ birth (first real conversations)

| Field | Value |
|---|---|
| Plan ID | TP-007a |
| Status | approved |
| Date | 2026-09-16 |
| Author | pi coding agent (LLM session) |
| Approver | Owner (session approval of the round-15 plan + the profile-tiered loop-cap ruling: standard = 10; recorded in AILOG-0021) |
| Approval date | 2026-09-16 (precedes implementation commits in the PR) |
| Target requirements | REQ-SI-FR-008 (pipeline); REQ-SI-FR-019 (streaming); REQ-SI-COST-002 (usage accounting); REQ-SI-INV-001/INV-002 (wired enforcement); REQ-SI-GOV-003 (prompts/ v1 + version stamps) |
| Implementation PR | (filled when the PR opens; body must contain "Test-Plan: TP-007a") |

## Scope

**Changes**: `agent/loop.py` (TurnEngine: §7.2 pipeline — bounded tool loop with profile-tiered caps quick=4/standard=10/deep=20, values-as-seen snapshots as the INV-001 pool, guardrail wiring with quarantine events (`post_check: failed` + original archived) and regeneration via provider-backed closure, usage accounting per event, rendering contract: stream deltas live, render only verdict-changed text); `agent/providers.py` (`complete(messages, tools, stream_sink)` — one code path, streaming aggregation with tool-call delta assembly, non-streaming tool-call parsing; `ChatOutcome`); `agent/registry.py` (`openai_tool_specs()` — spec → OpenAI function format); `shared/tools.py` (`JSON_SCHEMA_TYPES`); `agent/session.py` (`read_events` — read-only replay); `agent/profiles.py` (`TOOL_LOOP_LIMITS` + `tool_loop_limit`); `agent/repl.py` (free text → TurnEngine with streaming sink; unconfigured-provider explicit error with remediation; Ctrl+C mid-turn = interrupted, session survives; prompt-version stamp completes GOV-005's third field); `prompts/identity.md` (v1: role, five disciplines — English-only, epistemic safety, numbers-from-tools, tools-as-only-data-path, explicit failures); trace-matrix FR-008 row gains `test_agent_loop.py`; tests: `test/offline/test_streaming.py` (matrix-named, FR-019), `test/offline/test_agent_loop.py` (new), persistence extension (usage roundtrip), CLI-surface extension (unconfigured-provider explicitness).

**Explicitly does not change (deferred to TP-007b)**: `/show` `/resume` (FR-022/023); compaction; report-level eval cases (E-002); reasoning/report stream separation (interface reserved); history-window configurability.

## Planned tests

| Test file | Verifies (REQ-ID) | Kind | Notes |
|---|---|---|---|
| `test_streaming.py::test_first_delta_arrives_before_completion` / `...in_order` | REQ-SI-FR-019 | positive | S1: first delta strictly before the completion marker |
| `...::test_tool_phase_then_streamed_text` | REQ-SI-FR-019 | positive | tool phase emits no text deltas; text streams after |
| `...::test_real_provider_sets_stream_flag_and_aggregates` / `...streams_tool_call_deltas` | REQ-SI-FR-019, REQ-SI-FR-021 | positive | real provider path with fake clients: stream flag, delta assembly, usage |
| `test_agent_loop.py::test_tool_loop_end_to_end` | REQ-SI-FR-008, REQ-SI-INV-001 | positive | scripted provider → real registry → snapshot → cited number passes post-check; event kind sequence; usage totals |
| `...::test_fabricated_number_quarantined` | REQ-SI-INV-001 | negative/adversarial | 999 → degraded display; error event archives the original |
| `...::test_epistemic_regeneration` | REQ-SI-INV-002 | positive+negative | violation → provider-backed regeneration recovers; usage accumulates both calls |
| `...::test_three_strikes_aborts` | REQ-SI-INV-001 | boundary | third consecutive failure aborts with explicit message |
| `...::test_iteration_cap_per_profile` | REQ-SI-FR-020 | boundary | deep cap 20 / quick cap 4 reached; explicit stop, nothing fabricated |
| `...::test_history_window_bounded` | REQ-SI-FR-011 | positive | 20-event window mapped; system prompt first; current input last |
| `...::test_clean_text_streams_without_double_render` | REQ-SI-FR-019 | negative | streamed text is not re-rendered |
| `...::test_prompt_version_loading` | REQ-SI-GOV-003 | positive | front-matter → `identity-v1` stamp |
| `test_session_persistence.py::test_usage_event_roundtrip` | REQ-SI-COST-002 | positive | usage survives the append-only store |
| `test_cli_surface.py::test_repl_free_text_with_unconfigured_provider_is_explicit` | REQ-SI-INV-003 | negative | actionable error; slash commands unaffected |

## Negative tests (mandatory, R7)

Fabricated-number quarantine; three-strike abort; iteration-cap explicit stop; unconfigured-provider refusal; no-double-render (streaming correctness as a negative).

## Boundary cases (R8, for threshold rules)

Profile-tiered caps exercised at their limits (deep=20 reached; quick=4 reached; standard covered by the cap constant and end-to-end default); three-strike streak boundary; history-window boundary (exactly one window + current turn).

## Fit-criteria mapping

- **FR-019**: first-delta-before-marker asserted directly; deltas in order; tool/text phases separable.
- **FR-008 (pipeline)**: end-to-end scripted turn exercises assembly → membrane → snapshot → guardrail → persistence in the exact §7.2 order.
- **INV-001/002 (wired)**: quarantine archives originals; regeneration recovers; counters abort.
- **COST-002**: usage fields on assistant events, accumulated across regeneration.
- **GOV-003**: identity-v1 stamped and loadable; eval gate fires on this PR (providers + prompts touched) — the seed set replays.

## Regression scope

Full offline suite (all prior expectations unchanged — /tools, config, sessions, guardrail, registry suites untouched in semantics). Structural gates; docstring gate on the new module; the eval gate runs by design on this change set.

## Rulings recorded

Profile-tiered loop caps (owner: standard=10; quick=4/deep=20 agent-proposed, owner-approved in plan); INV-001 snapshot = the turn's ToolResult payloads (strict); rendering contract as specified in loop.py's docstring.
