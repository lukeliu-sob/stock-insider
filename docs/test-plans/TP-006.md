# Test Plan TP-006 — Runtime guardrail: INV-001 post-check, INV-002 filter (ADR-006)

| Field | Value |
|---|---|
| Plan ID | TP-006 |
| Status | approved |
| Date | 2026-09-16 |
| Author | pi coding agent (LLM session) |
| Approver | Owner (session approval of the round-14 plan and its five rulings, recorded in AILOG-0020) |
| Approval date | 2026-09-16 (precedes implementation commits in the PR) |
| Target requirements | REQ-SI-INV-001; REQ-SI-INV-002; REQ-SI-INV-003 (language surface) |
| Implementation PR | (filled when the PR opens; body must contain "Test-Plan: TP-006") |

## Scope

**Changes**: `agent/guardrail.py` (safety-critical verdict library): INV-001 numeric post-check (exact-after-normalization matching, thousands-separator/int-float canonicalization, nested snapshot walking, derived-number refusal); INV-002 epistemic filter (deterministic-prediction and causal-claim pattern families with past-tense coverage, hypothesis-marker survival, sentence-level stripping) plus the one-regeneration-then-refuse flow with injected regenerator; combined `run_postcheck` verdict (quarantine precedence, degradation statement, language surface); `PostCheckCounter` (three-strike numeric, >2 epistemic budgets). `docs/architecture/adr/006-guardrail.md`. Two trace-matrix-named test files born: `test/offline/test_inv1_postcheck.py`, `test/offline/test_inv2_filter.py`.

**Explicitly does not change**: pipeline wiring (quarantine event storage, degraded display, session abort actions — TP-007); live eval-set epistemic cases and precision/recall recording (loop era, honest-measurement ruling); INV-004 symbol gate (watchlist TP); per-number source attribution (provenance-ledger TP); registry/providers/session behavior.

## Planned tests

| Test file | Verifies (REQ-ID) | Kind | Notes |
|---|---|---|---|
| `test_inv1_postcheck.py::test_fabricated_number_rejected` | REQ-SI-INV-001 | negative/adversarial | 42.0 vs 311.4 snapshot → fail |
| `...::test_cited_number_passes` | REQ-SI-INV-001 | positive | exact citation passes |
| `...::test_boundary_plus_minus_one_fails` | REQ-SI-INV-001 | boundary (R8) | 311.4 vs 311.5 fails; 311.5 passes |
| `...::test_multiple_failures_all_listed` | REQ-SI-INV-001 | negative | every failed numeric named |
| `...::test_thousands_separator_normalized` / `...::test_int_float_equivalence` / `...::test_nested_snapshot_walk` / `...::test_string_values_are_walked` | REQ-SI-INV-001 | positive | normalization + pool construction |
| `...::test_zero_numbers_passes_trivially` / `...::test_extract_numbers_shapes` | REQ-SI-INV-001 | positive | extraction semantics |
| `...::test_verdict_quarantines_and_degrades` / `...::test_verdict_clean_pass` / `...::test_language_violation_quarantines` | REQ-SI-INV-001/003 | negative + positive | verdict composition and precedence |
| `...::test_counter_three_strikes_then_abort` / `...::test_counter_resets_on_pass` | REQ-SI-INV-001 | negative + boundary | streak semantics |
| `test_inv2_filter.py::test_deterministic_prediction_blocked` / `...guaranteed` / `...must_pattern` / `...causal_claim` | REQ-SI-INV-002 | negative/adversarial | all pattern families incl. past tense |
| `...::test_labeled_speculation_passes` / `...::test_unlabeled_prediction_with_might_still_flagged_only_when_pattern` | REQ-SI-INV-002 | positive | labeling requirement honored |
| `...::test_clean_analysis_passes` / `...::test_strip_removes_only_violations` | REQ-SI-INV-002 | positive | surgical stripping |
| `...::test_regeneration_recovers` / `...::test_regeneration_still_bad_refuses_with_safe_summary` | REQ-SI-INV-002 | positive + negative | fallback semantics 1-2-3 |
| `...::test_two_violation_budget_aborts` | REQ-SI-INV-002 | boundary (R8) | >2 cumulative violations abort |
| `...::test_quarantine_takes_precedence_in_display` | REQ-SI-INV-001/002 | negative | composed precedence |

## Negative tests (mandatory, R7)

Seventeen negative/adversarial cases across the two files; both suites are 0-escape by design (fabricated numbers never pass; violating outputs never survive the filter unlabeled).

## Boundary cases (R8, for threshold rules)

±1 numeric boundary (311.4 vs 311.5); the >2 epistemic budget boundary (2 violations tolerate, 3 abort); the three-strike streak boundary with mid-streak reset.

## Fit-criteria mapping

- **INV-001**: fabricated-number 0-escape + correctly-cited 100% pass + ±1 boundary — the fit criterion's three clauses verbatim; quarantine/degradation/streak semantics per the invariant fallback text.
- **INV-002**: 0 violating outputs pass the filter; labeled speculation survives; strip-once-regenerate-once-refuse flow with hypothesis-labeled summary; >2 budget abort.
- **INV-003 (language)**: non-English candidates quarantine with an explicit withheld statement.

## Regression scope

Full offline suite (TP-001..TP-005 expectations unchanged; no behavior changes outside the new module). Structural gates: import boundaries (guardrail imports shared only — legal edge), docstring gate, language gate (test fixtures use English + the exempted policy-test file only), secret scan.

## Amendment 2 (2026-09-21) — BD-012: display-rounding match

The numeric postcheck gains a bounded display-rounding allowance
(token with exactly 2-4 decimals matches a pool value within half an
ulp of that place; tracked as `rounded`). Four new tests pin the
edges: two-decimal rendering passes; near-misses (...23, ...241,
one-decimal) and integer renderings fail; the ±1 adversarial class
is unchanged and must keep failing. identity.md v3 adds the exact
rendering discipline (GOV-003 replay runs in CI).
