"""Deterministic computation: indicators, event study, statistics (TP-013).

The ONLY module where market-data arithmetic happens (FR-006; static
gate enforced). Scaffold state: package shell only.

Implements: REQ-SI-FR-006 (ADR-001)
"""
