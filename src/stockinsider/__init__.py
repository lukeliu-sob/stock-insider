"""Stock Insider — local CLI financial analyst agent for HK + US equities.

Package root: version metadata and product identity. The advisory side
(agent/) and the authoritative data side (data/) are joined by the CLI
shell (cli/); analysis only, never trades.

Implements: REQ-SI-FR-013 (ADR-001)
"""

__version__ = "0.1.0"
