# AI 代理时代的需求工程方法论
## ——面向已有系统改进的通用实践指南

> 适用对象：对已有系统（含开源项目、遗留系统、在运营产品）实施改进的团队或个人，尤其是引入 AI 能力或引入 AI 编码代理工作流的场景。
> 本文是自包含的通用方法论，不依赖任何特定课程或教材。

---

## 一、核心主张（原则层）

1. **规格即源代码**：当 AI 代理承担代码生成时，需求规格成为唯一的人工输入；规格的质量上限就是系统的质量上限。规格缺陷会被 AI 忠实而大规模地放大。
2. **单一事实来源**：需求登记册（机器可读、可版本控制）是人与 AI 共同的唯一事实来源；规格含糊或缺失时，AI 不会停下来提问，而是"自信地猜"。
3. **AI 提议，人类决策**：AI 可以读、写、总结、起草、检测，但每个候选需求与每次放行必须由人验证与批准；无人批准不得发布。
4. **不变式由确定性代码执行**：安全、合规类的关键规则不可协商、不可配置、绝不写进提示词；AI 输出只做建议，确定性代码做最终裁决。
5. **一切可验证**：每条需求必须有机器可检查的完成判据（fit criterion），它将直接转化为测试；模糊的判据只能产出模糊的代码。
6. **全生命周期观**：需求不止于部署——变更、运行监控、反馈回流、退役都在管理范围内。
7. **失败即安全**：不确定、过载、非法输出时永远进入安全默认（拒绝/转人工），绝不放行。

---

## 二、开始前的两个判定

**判定 1：系统与 AI 的关系**

| 类型 | 特征 | 审计重点 |
|---|---|---|
| A 类 | 用 AI 代理辅助开发，系统本身无 AI 功能 | AI 生成代码的质量、评审、回归、追溯 |
| B 类 | 系统本身是代理/LLM 应用 | 非确定性、注入、工具误用、成本、可解释性 |
| C 类 | 两者兼有 | 两张清单全部审计 |

**判定 2：改进目标**

- **补需求债**：系统已有行为从未被写成规格 → 重点在 Step 1–3；
- **引入 AI 能力**：新增 AI 功能 → 重点在 Step 0 的义务映射、Step 3 的 AI 需求四类与不变式；
- **治理化**：让系统可追溯、可审计、可安全演化 → 重点在 Step 2、5、6、7。

---

## 三、方法论总览

```
Step 0 域与干系人分析
   ↓
Step 1 逆向需求引出（恢复现状 + 找缺口）      ┐
   ↓                                          │
Step 2 建立基线工件（五件套）                  │  交
   ↓                                          │  互
Step 3 规格化（登记册 + 分类 + 判据）          │  迭
   ↓                                          │  代
Step 4 验证与协商（多源印证 + 优先级 + 边界）  │
   ↓                                          │
Step 5 追溯与回归门（CI 集成）                ┘
   ↓
Step 6 运行时监控与反馈环（服务型系统）
   ↓
Step 7 需求退役协议（替换旧行为时）

横切：变更管理、决策记录（ADR）、术语表治理
```

---

## 四、步骤详解

### Step 0｜域与干系人分析

**目标**：在写任何需求前理解环境，避免"拿着方案找问题"。

**活动**：
1. 五维环境扫描：**监管与政策**（适用法规、平台政策、数据合规）、**治理结构**（决策权、审批流、维护团队构成）、**用户文化**（使用习惯、语言、隐私期望）、**技术约束**（既有架构、集成边界、性能预算）、**资源约束**（时间、预算、人力）。
2. 干系人视点表：为每类干系人列出其"看到的需求集合"。五类视点：

| 视点 | 关注点 | 典型需求形态 |
|---|---|---|
| 终端用户 | 速度、准确、易用 | 功能需求、性能、可用性 |
| 监管/合规方 | 安全、审计、留痕 | 不变式、治理需求 |
| 开发/维护者 | 可测试、可维护、接口稳定 | 架构约束、回归保护 |
| 资助方/客户 | 价值、公平、隐私 | 范围与商业规则 |
| **AI 编码代理（新视点）** | 机器可读规格 | 结构化判据、权限模型、术语表 |

3. （引入 AI 能力时）**义务映射**：列出所有适用的法规与政策义务，逐条翻译为具体需求。规则：**每条义务至少映射一条需求，漏映射即"设计上不合规"**；一条义务常拆出多条需求。

**产出**：干系人视点表、环境扫描笔记、义务-需求映射表。
**常见错误**：跳过环境扫描直接写功能清单；忽略 AI 代理这个"新干系人"。

### Step 1｜逆向需求引出

**目标**：已有系统的需求大部分已隐含在代码、测试、工单和讨论里——先"恢复现状"，再"找缺口"。

**活动**：
1. **双来源收集**：
   - **物**：源码与测试（隐性需求的载体）、文档、CI 配置、issue/PR 历史（组织记忆）；
   - **人**：维护者、高频贡献者、真实用户（工单里的痛点与 workaround）。
   - 原则：**漏掉一个来源 = 漏掉需求 = 交付物中的缺陷**。
2. **AI 辅助起草**：让 AI 读代码/文档/工单，提取事实、聚类需要、起草候选需求、标记缺口。每个 AI 产出必须经过**五步人工验证**：阅读 → 质疑 → 验证 → 修订 → 批准。
3. **风险审计清单法**：用第 7.2 节的"AI 风险审计清单"逐条自问："本系统有没有对应的需求、护栏或测试？"
4. **补充技术**（按情境选择）：
   - 关键路径任务分解（自顶向下拆到动作与事件）；
   - 场景走查（走一遍核心用户旅程，每个 what-if 都记为候选需求）；
   - 头脑风暴（主题设为"哪些缺口会导致清单中的问题"）；
   - 实地观察用户真实操作（发现说不出口的隐性知识）。

**产出**：候选需求列表，每条附来源链接（文件、工单、讨论）。

### Step 2｜建立基线工件（五件套）

```
spec/
├── requirements.yaml   # 需求登记册：单一事实来源
├── glossary.md         # 领域术语表：术语 → 代码常量映射
├── trace-matrix.csv    # 追溯矩阵：需求 → 组件 → 测试 → 监控
├── adr/                # 决策记录：权衡、变更、退役的理由
└── prompts/            # 含 AI 功能时：提示词规格（版本化）
```

**术语表规则**：每词一义；用领域真实术语而非泛化 IT 词；**每个术语映射到代码常量/函数**；需求写作只允许使用术语表词汇。不标准化的术语会导致人与 AI 各自理解（例如"超额"可指超计划限额、超日限额、超余额……至少五种解读）。

### Step 3｜规格化

**登记册条目模板**：

```yaml
- id: REQ-<系统名>-<类型>-<序号>        # 稳定 ID，贯穿需求/架构/测试/代码
  type: functional | invariant | quality | governance | security | performance | cost
  statement: "一句话陈述，精确无歧义"     # 禁用 should / try to / usually
  fit_criterion: "可测规则 + 阈值 + 验证方式"   # 必须机器可检查
  source: "来源链接/工单/文档"           # 溯源
  status: implemented | conceptual | future | deprecated
  depends_on: []                         # 可选：依赖的其他需求
  regression_trigger: true               # 变更时必跑回归
  regression_scope: [test_xxx, ...]      # 指定回归测试集
```

**分类要点**：
- **功能需求（FR）**：系统必须计算/执行什么（WHAT，不写 HOW）；
- **不变式（INV）**：不可协商、由确定性代码强制执行、永不通过提示词承载、永不可配置；
- **质量/性能/成本/安全/治理**：分别对应可测质量属性、延迟与吞吐目标、token/资源预算、攻击防御、可追溯与审批权。

**含 AI 功能的系统必配四类新需求**：

| 类别 | 需求要点 |
|---|---|
| 成本治理 | 预算（均值目标）+ 硬上限 + 溢出动作（摘要重试→转人工）+ 监控告警；红线：不得以牺牲正确性与安全换成本 |
| 输出质量 | 忠实度/相关性等指标阈值化（如 ≥0.6），低分触发升级人工 |
| 对抗鲁棒性 | 三层纵深防御：输入消毒（进入模型前中和注入）→ 输出校验（schema 校验、畸形即失败关闭）→ 确定性策略执行（最终裁决）；任何单层不可被绕过 |
| AI 治理 | 模型/提示词/阈值版本随每次决策入日志；安全文件修改需决策记录（ADR）；人工监督与升级路径 |

**性能需求两要素**：可测量目标（分阶段延迟预算、分位数值）+ 超阈值回退（降级→复查→失败关闭转人工）。

**写完自查**（见 7.1 节规格质量检查）。

### Step 4｜验证与协商

**验证**（按阶段组合使用）：
1. 早期：与干系人结构化走查（公开项目 = RFC/issue 讨论）；
2. 中期：原型验证（spike 分支/PR）；
3. 迭代中：测试计划即验证（判据先于实现）；
4. 交付时：用户验收/发布反馈。

**多源印证**：同一候选需求至少两个独立来源支撑；警惕仅有间接来源（转述）的需求——信息经转手会失真；来源越多越可靠（收益递增到某点后趋平）。

**协商三清单**：系统**必须做**什么、**必须不做**什么、**什么在范围外**。写明三清单的意义：干系人目标天然冲突，不协商则需求不一致，AI 编码代理最终无法产出稳定实现。协商发生在需求引出与优先级排序两个阶段；分析师不是旁观者——要提出尖锐问题、呈现权衡、引导达成协议。

**优先级**：
- 推荐四分法——**Must**（缺则系统不安全/不合规/不可用：不变式、法律义务、核心功能）、**Should**（重要但有替代方案）、**Could**（增强项，延后无即时危害）、**Won't**（本版明确不做，但干系人知情）；
- 排序原则：安全与合法性优先；基于风险与价值；**每条需求必须显式标注优先级**；定期重访；理由写入 ADR。
- 经验排序：不变式 ≥ Must；成本类多为 Could（成本高一点系统仍可运行）；装饰类为 Won't。

**显式权衡**（识别 → 评估 → 决策并记录，否则 AI 会不一致地猜）：
| 权衡轴 | 决策要回答 |
|---|---|
| 成本 ↔ 质量 | 是否接受更高资源成本换取质量阈值达标 |
| 速度 ↔ 安全 | 是否为安全保证牺牲响应速度 |
| 自动化 ↔ 监督 | 哪些决策交机器、哪些必须人审 |
| 功能丰富 ↔ 稳定 | 用什么机制渐进交付（特性开关、灰度） |

### Step 5｜追溯与回归门

**追溯矩阵**：每条需求登记两列——**责任组件**（哪个代码单元执行它）与**验证方式**（哪个测试/检查证明它）。没有追溯的需求等于没有实现承诺。

**CI/CD 回归门**：
1. CI 检测需求登记册的 Git diff，自动触发变更条目 `regression_scope` 内的测试；
2. 门决策：全部通过 → 允许合并；任一失败 → 阻断，修复后重跑；
3. 三条纪律：
   - **新增需求只加行、不改旧行**；
   - 旧行失败即回归——**修代码，不改测试**；
   - 边界类需求必带边界±1 测试；不变式必带对抗测试（目标：零逃逸）。

**每次需求变更五问**：规格改了哪些条目？哪些上游/下游工件受影响（架构、测试、监控、文档）？评审门是否仍然完好？是否需要新回归测试或调整人工审核设计？所有文档是否同步？（过时段落会直接误导 AI 编码代理。）

### Step 6｜运行时监控与反馈环（服务型系统）

**三类指标**：
1. **过程质量**：需求波动率（每迭代新增/变更占比）、响应性（变更到规格更新的时长）、缺陷密度、回归覆盖率（应有回归测试的功能需求占比）、歧义率（无清晰验收标准的需求占比）；
2. **运行时**：输出质量指标漂移（滚动窗口对比基线）、成本（单次调用量对比基线）、护栏触发率（异常尖峰立即评审）、延迟（分位数持续超标即升级）、数据质量（缺字段率、不支持输入比例）；
3. **人类反馈**：对 AI 决策的接受/拒绝率、质量评分、人工改写内容、评论、新需求想法。

**行动闭环六步**：检测 → 分析根因与受影响范围 → 决策（更新需求/提示词/阈值/架构，写入 ADR）→ 更新工件（登记册、提示词、阈值、测试、配置）→ 再验证（评估门 + 回归）→ 部署并继续监控。

**铁律**：每个告警必须导致一次调查与决策；监控闭合"需求与现实之间的环"。

### Step 7｜需求退役协议

替换或删除旧行为时，**永不静默删除**。五步协议：

1. **标记弃用**：状态改为 deprecated，记录原因与日期，指明 `superseded_by`（被哪条新需求取代）；
2. **通知**：所有受影响的负责人知情，相关决策记录重开修订；
3. **归档**：移入弃用区，保留完整历史供审计与学习；
4. **移除**：弃用窗口（至少一个发布周期；合规条目按法定保留期）期满后，删除关联测试与代码，并验证生产环境无残留依赖；
5. **监控**：防止隐藏引用残留。

**守卫测试技巧**：为被替换的旧行为写一个"在新系统中应当失败"的守卫测试——它若还能通过，说明旧行为仍残留在系统中，删除前必须处理。

---

## 五、含 AI 功能时的附加规格

### 提示词即需求工件

- 提示词是版本化制品：任何提示词变更等同需求变更，必须评审、测试、重新批准；
- 生命周期六步：起草 → 测试（含边界例）→ 评审（像代码一样）→ 版本化 → 部署（带追溯与监控）→ 再评估（任何提示词或模型变更后重测）。

**提示词六组件**：系统消息（角色+严格输出格式）｜上下文（必要规则与数据）｜任务指令（明确步骤）｜示例（正常+边界）｜推理引导（分步思考）｜负向约束（禁止事项与失败关闭策略）。

**六项检查**：安全性（禁令与护栏）｜结构（严格输出 schema）｜语气（事实性、不发挥）｜边界情况（缺数据/畸形输出的安全默认）｜版本化｜依赖声明（模型、参数、引用规则）。

### 误用案例（AI 功能必写）

攻击视角五步转化：**识别攻击向量 → 派生防御需求 → 设计护栏 → 写对抗测试 → CI 门禁验证**。典型向量：指令覆盖、诱导幻觉配置、畸形输出触发不安全默认、多语言注入、超长输入耗尽资源。

### 上下文工程（长输入成本控制）

流程：估算 token → 预算内直接用；超预算 → **分块 → 逐块摘要 → 合并**（保留关键事实：金额/类型/日期/决策要素，"保事实不保散文"）；摘要后仍超 → 转人工或确定性回退。辅助策略：相似键缓存（策略或提示词变更时必须失效缓存）、选择性检索（只取相关节）。

---

## 六、时间有限时的最小落地路径（5 件事）

1. 建需求登记册：先登记 10–20 条**现状**核心需求（AI 起草 + 人批准），配术语表；
2. 用 AI 风险审计清单做一次缺口审计，缺口写成 status: future 的需求（含完成判据）；
3. 最高风险的 3 条写成**不变式**，配边界 + 对抗测试，接入 CI；
4. 建追溯矩阵（需求→组件→测试），此后每次提交必须携带追溯；
5. 所有需求变更走 `regression_trigger` + 决策记录（ADR）。

---

## 七、检查清单汇总

### 7.1 规格质量检查

**七大反模式**（写需求时逐条排除）：
| # | 反模式 | 典型症状 |
|---|---|---|
| 1 | 噪声 | 混入无关的界面/品味描述 |
| 2 | 沉默 | 已知场景在规格中只字不提 |
| 3 | 过度规格 | 规定了实现技术而非问题本身 |
| 4 | 矛盾 | 两条需求互相冲突 |
| 5 | 歧义 | 一句话多种解读 |
| 6 | 前向引用 | 引用尚未编写的内容 |
| 7 | 一厢情愿 | 写下无法验证/无法达成的目标 |

**六项质量标准**：正确性、完整性、一致性、精确性、可读性、**可测试性**（每条需求有可测判据）。

**判据写作三问**：能用测试证明通过/失败吗？有明确阈值吗？有边界值测试方案吗？

### 7.2 AI 风险审计清单

**组 A：用 AI 代理开发系统时**
1. 权威崩塌：AI 产物未经确定性后检即被采纳？
2. 静默回归：AI 重构悄悄删除/破坏既有行为？
3. 空转测试：AI 生成的测试无实质断言、轻松全绿？
4. 注意力衰减：长迭代中约束逐渐失效？
5. 出处丢失：需求→提示词→代码→测试无端到端追溯？
6. 护栏绕过：生成代码可绕开安全包装？
7. 幻觉：调用不存在的接口/库？
8. 评审疲劳：人类对 AI 产物橡皮图章式批准？
9. 指标漂移：模型/提示词随时间退化且无告警？
10. 结构劣化：过度工程、代码膨胀、高耦合？

**组 B：构建 AI/代理原生应用时**
11. 非确定行为：同输入不同输出，无一致性边界？
12. 工具误用：代理调错工具/参数或调用幻觉工具？
13. 状态损坏：检查点与存储混淆、重启丢状态？
14. 多代理协调：死锁、竞态、共享状态污染？
15. 成本失控：token 预算无上限、无监控、无降级路径？
16. 提示注入：用户输入可覆盖系统指令？
17. 不可解释：无法回答"系统为什么这么做"？
18. 人机回环退化：审批量大时人工审核沦为形式？

### 7.3 需求变更五问（每次变更必答）

规格改了什么？哪些工件受影响？评审门是否完好？需要哪些新回归测试？文档是否全部同步？

---

## 八、黄金规则（十二条）

1. 规格是人与 AI 的单一事实来源；不精确，AI 就猜。
2. 别让 AI 猜——把所有决策写进规格。
3. AI 提议，人类验证与决策；无人批准不发布。
4. 每条外部义务至少映射一条需求；每条需求必须可验证。
5. 漏掉一个需求来源 = 漏掉需求 = 交付物中的缺陷。
6. 模糊的完成判据产出模糊的代码。
7. 改提示词就是改需求：评审、测试、重新批准。
8. 变更不可避免，失控的变更才危险。
9. 旧行为失败即回归——修代码，不改测试。
10. 不确定或过载时永不放行；失败即进入安全默认。
11. 需求不止于部署；监控闭合需求与现实的环。
12. 需求退役不是删除，而是可追溯的治理过程。

---

## 九、演化路线建议

为遗留系统引入 AI 能力时，按四阶段演进，**不要跳到"裸用 AI 输出"阶段**：

1. **规则内核**：核心业务规则以确定性代码沉淀，边界清晰；
2. **策略抽离**：将业务规则抽成独立策略层，对外形成稳定接口（信息隐藏：改动策略不破坏调用方）；
3. **AI 建议**：AI 参与解读与建议，但其输出仅作为输入传给裁决层——**此阶段切忌让 AI 输出直接生效**（权威崩塌与幻觉风险最高）；
4. **受护栏混合**：AI 提议 + 确定性代码裁决 + 三层防御（输入消毒/输出校验/策略执行）+ 全程可审计——这是安全引入 AI 的目标形态。

---

*使用方式：将本文作为项目 CONTRIBUTING/SPEC 体系的骨架，按第二节判定裁剪步骤；所有模板（登记册、术语表、追溯矩阵、ADR、提示词规格）可直接复制进 `spec/` 目录使用。*
